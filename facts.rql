facts := READ_CSV("https://KingSebastian.github.if/git-cloud-storage/Facts.csv");

// Number of airports per country.
fun_fact(id: string) := {
    SELECT FACT
    FROM facts
    WHERE facts.ID = id
}
