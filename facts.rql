facts := READ_CSV("https://KingSebastian.github.io/git-cloud-storage/Sample/Facts.csv");

// Number of airports per country.
fun_fact(id: string) := {
    SELECT FACT
    FROM facts
    WHERE facts.ID = id
}
