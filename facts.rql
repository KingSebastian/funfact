facts := READ("https://github.com/KingSebastian/funfact/blob/master/Sample.csv");

// Number of airports per country.
fun_fact(id: string) := {
    SELECT FACT
    FROM facts
    WHERE facts.ID = id
}
