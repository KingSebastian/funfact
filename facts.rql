facts := READ("https://raw.githubusercontent.com/KingSebastian/UsefullLinks/main/Sample.csv");

// Number of airports per country.
fun_fact(id: string) := {
    SELECT FACT
    FROM facts
    WHERE facts.ID = id
}
