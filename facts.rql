facts := READ_CSV("https://raw.githubusercontent.com/KingSebastian/funfact/Sample/Facts.csv");

// Number of airports per country.
fun_fact(id: string) := {
    SELECT FACT
    FROM facts
    WHERE facts.ID = id
}
