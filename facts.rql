fun_fact(name: string, role: string, fact: string) := {
SELECT * 
FROM READ("https://github.com/KingSebastian/UsefullLinks/blob/main/facts.json") f
WHERE 
    f.name like name and 
    f.role like role and 
    f.fact like fact
limit 10
};
